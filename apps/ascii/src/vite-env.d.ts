// Vite's ambient types. Here for `import.meta.env.PROD`, which is what keeps the service worker
// registration out of a dev server that never builds one (ADR 0027).

/// <reference types="vite/client" />
