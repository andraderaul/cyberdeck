---
'@cyberdeck/glitch': minor
---

The PRESETS row shows each look instead of only naming it. Every Preset chip now carries a
thumbnail of its Chain, rendered at build time over the deck's reference plate at the full 800px the
canvas samples at and shrunk only afterwards, so DEGAUSS' slow bend is a bend and VAPORWAVE still
leaves the picture readable — which is not what a Chain run straight into a 96px chip would show.
The accessible name is unchanged: the picture is `alt=""`, and `(modified)` is still spelled out.
