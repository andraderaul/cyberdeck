---
'@cyberdeck/ascii': minor
---

Dithering: a Bayer or Floyd–Steinberg pass over the sampled grid before the Charset buckets a cell, so a coarse Charset carries a gradient instead of banding it — pick it from the EDIT tab, `none` by default and unchanged from today.
