---
'@cyberdeck/deck-kit': patch
---

The Source Image drop zone lights its border on hover, matching the webcam panel it sits beside in the empty-state hero. Until now only the drop target reacted to a drag, so the left half of the hero read as dead to the mouse while the right half answered.
