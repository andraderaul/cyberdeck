---
'@cyberdeck/glitch': patch
---

Randomize goes icon-only on mobile, matching Re-roll — the ⚄ glyph alone below `sm`, the label back
from `sm` up. The accessible name stays "randomize" at both sizes.
