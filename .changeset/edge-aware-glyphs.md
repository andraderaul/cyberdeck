---
'@cyberdeck/ascii': minor
---

Edge Glyphs: where the local gradient reads as a contour, the cell takes a directional character instead of a brightness one — a shape axis beside the Charset's ramp, opt-in from the EDIT tab and off by default.
