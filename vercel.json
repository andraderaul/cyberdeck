{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace @cyberdeck/ascii",
  "outputDirectory": "apps/ascii/dist",
  "ignoreCommand": "[ \"$VERCEL_ENV\" != \"production\" ] && [ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ] && git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- apps/ascii packages/deck-kit package.json package-lock.json vercel.json ':(exclude)**/*.md'"
}
