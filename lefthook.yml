commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  commands:
    check:
      run: npm run check
    test:
      run: npm run test:run

pre-push:
  commands:
    typecheck:
      run: npm run typecheck
    test:
      run: npm run test:run
